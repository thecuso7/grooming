import type { VueWrapper } from "@vue/test-utils";

export const choosePetExist = async (wrapper: VueWrapper) => {
    
}

export const addNewPet = async (wrapper: VueWrapper) => {
    
}

export const chooseServiceComplex = async (wapper: VueWrapper) => {
    
}